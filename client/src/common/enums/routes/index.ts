enum Routes {
  DEFAULT = '/',
  LOGIN = '/login',
  CHART = '/chart',
  MATCHER = '/matcher',
  BUILDER = '/builder',
  SETUP = '/setup/:id',
  SETUPS = '/setups',
  USER = '/user/:id',
  ADMINTOOLS = '/admintools',
}

export { Routes };
