enum Routes {
  DEFAULT = '/',
  LOGIN = '/login',
  CHART = '/setup/:id/chart',
  MATCHER = '/matcher',
  BUILDER = '/builder',
  SETUP = '/setup',
  SETUPS = '/setups',
  USER = '/user/:id',
  GAME = '/game/:id',
}

export { Routes };
