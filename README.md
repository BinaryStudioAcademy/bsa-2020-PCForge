# PCForge

### Project setup

- ##### Client

  `$ cd client`
  `$ yarn` or `npm install`
  `$ yarn start` or `npm start`
  Set `.env` appropriately

- ##### Server
  `$ cd server`
  `$ yarn` or `npm install`
  `$ yarn dev` or `npm run dev`
  Set `.env` appropriately
